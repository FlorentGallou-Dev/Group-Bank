    <div class="container col-6 col-sm-12 bg-nav">
        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="#"></a>
                <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse bg-nav px-1 px-sm-0" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        {% if app.user %}
                            <li class="nav-item">
                                {# Checking current user "sex" to know how to call them #}
                                <p class="text-light px-lg-3 py-2 m-0">
                                    Bonjour, {% if app.user.sex is same as "Male" %} Monsieur {% elseif app.user.sex is same as "Female" %} Madame {% else %} {% endif %}{{app.user.lastname}} {{app.user.firstname}}
                                </p> <!-- ajouter attributs firstname et lastname du User -->
                            </li>
                        {% endif %}
                        <li class="nav-item text-uppercase">
                            <a class="nav-link active" aria-current="page" href="{{ path("accountsList") }}">Mes comptes</a>
                        </li>
                        <li class="nav-item dropdown text-uppercase">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Opérations
                            </a>
                            <ul class="dropdown-menu bg-sombre" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="{{ path("addAccountPage") }}">Nouveau compte</a></li>
                                {# <li><a class="dropdown-item" href="{{ path("closeAccountPage") }}">Cloture</a></li> #}
                                {# <li><a class="dropdown-item" href="{{ path("depositWithdrawalPage") }}">Dépot</a></li>
                                <li><a class="dropdown-item" href="{{ path("depositWithdrawalPage") }}">Retrait</a></li> #}
                                <li><a class="dropdown-item" href="{{ path("transferPage") }}">Transfert</a></li>
                            </ul>
                        </li>
                        <li class="nav-item text-uppercase">
                            <a class="nav-link" href="{{ path("statisticsPage") }}">Statistiques</a>
                        </li>
                        <li class="nav-item text-uppercase">
                            <a class="nav-link" href="{{ path("blogPage") }}">Actualités</a>
                        </li>
                        <li class="nav-item text-uppercase">
                            <a class="btn btn-dark mx-0 mx-lg-3" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</header>