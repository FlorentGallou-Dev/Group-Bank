{% extends 'base.html.twig' %}

{% block title %}Group Bank - Dêpot - Retrait {% endblock %}

{% block body %}
    <div class="row">
        <h1 class="color-sombre col-12 mb-5">Effectuer un {{ ope }} sur {{account.accountType}}</h1>
        <section class="container col-10 row mx-auto" id="postContainer">
            <div class="form-group row">
                {{ form_start(form) }}
                    <div class="my-custom-class-for-errors">
                        {{ form_errors(form) }}
                    </div>
                    <div class="row">
                        <div class="col-12 mb-3" id="some-custom-id">
                            {{ form_row(form.label) }}
                        </div>
                        <div class="col-12 mb-3" id="some-custom-id">
                            {{ form_row(form.operation_amount) }}
                        </div>
                        <div class="col-12 mb-3">
                            {{ form_row(form.enregistrer) }}
                        </div>
                    </div>
                {{ form_end(form) }} 
            </div>
        </section>
        </div>
{% endblock %}